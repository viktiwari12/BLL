
import java.util.*;
public class ques {
    public static void main(String[] args) {
        
    }
    // Given a array, and a value k 

    // Print the kth largest element of the array.

    // For ex-> if k == 3 
    //Print 3rd largest element f the array 

    /* 
     * Sort the array  & return the arr[n-k]th element  
     *                  
     */
    public static void kthLargest(int arr[],int k){


        // Create a max heap O(1)
        // put all the elemnts in the max heap.  N-> logN -> NlogN (time )
        // start removing the element & the kth element is the ans K -> KlogN (time )

        // O(N) -> Space 

        /* Complexity analysis */
        
    }
}
