

public class l001 {
    // Node class
    public class node{
        int data;
        node next;

        node(){};
        node(int data){
            this.data = data;
            this.next = null;
        }
        node(int data, node next){
            this.data = data;
            this.next = next;
        }
    }
    public static void main(String[] args) {
        display(head:node){ 
    }
    // give a LinkedList -> Display all the elemetns 
    public static void display(node head){
        
        while(head!=null){
            System.out.println((head.data));

            // Updationn  isMissing 
        }

    }
}
    // Given A LinkedList -> Find the mid node
    public static node mid(node head){

    }

    // GIve A LinkedList 
    // Add a element 
    /* 
     * -> At the first 
     * -> At the Laast 
     * -> At any random location 
     */

     public static void addFirst(node head, int tar){

     }
     public static void addLast(node head, int tar){
        
     }
     public static void addAtK(node head, int tar, int k){
        
     }
}
